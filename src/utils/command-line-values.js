const FILTER = "--filter=";
const COUNT = "--count";
const ERROR =
  "The correct usage is node app.js --filter=VALUE, or node app.js --count, or node app.js --filter=VALUE --count";

module.exports = { FILTER, COUNT, ERROR };
